<p id="notice"><%= notice %></p>

<h1>Lista de Jogos</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Plataforma</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jogos.each do |jogo| %>
      <tr>
        <td><%= jogo.nome %></td>
        <td><%= jogo.plataforma %></td>
        <td><%= jogo.categoria %></td>
        <td><%= link_to 'Detalhes', jogo %></td>
        <td><%= link_to 'Atualizar', edit_jogo_path(jogo) %></td>
        <td><%= link_to 'Excluir', jogo, method: :delete, data: { confirm: 'Confirmar exclusão?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar Jogo', new_jogo_path %>
<%= link_to 'Voltar', root_path %>
