<p id="notice"><%= notice %></p>

<h1>Lista de Clientes</h1>

<table>
  <thead>
    <tr>
      <th>CPF</th>
      <th>Nome</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clientes.each do |cliente| %>
      <tr>
        <td><%= cliente.cpf %></td>
        <td><%= cliente.nome %></td>
        <td><%= cliente.email %></td>
        <td><%= link_to 'Detalhes', cliente %></td>
        <td><%= link_to 'Atualizar', edit_cliente_path(cliente) %></td>
        <td><%= link_to 'Excluir', cliente, method: :delete, data: { confirm: 'Confirmar exclusão?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar Cliente', new_cliente_path %>
<%= link_to 'Voltar', root_path %>
